prometheus:
    prometheusSpec:
        storageSpec:
            volumeClaimTemplate:
                spec:
                    resources:
                        requests:
                            storage: 10Gi
grafana:
    adminUser: admin
    adminPassword: "admin"